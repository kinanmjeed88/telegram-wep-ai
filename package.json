{
  "name": "techtouch-channels-serverless",
  "version": "1.0.0",
  "description": "Dependencies for Netlify Functions",
  "scripts": {
    "build": "echo 'No frontend build step required. Netlify will handle function bundling.'"
  },
  "dependencies": {
    "@google/genai": "0.14.0",
    "@netlify/functions": "^2.6.0"
  }
}
